Ducatus Node assembly script

To install all prerequisites:

```
./prebuild_api_node.sh
```

To create ducatus node in upper directory:

```
./build_api_node.sh
```

Configuration file will be taken from `node_files`

